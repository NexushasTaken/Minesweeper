#ifndef H_UTILS
#define H_UTILS

#include <raylib.h>

void printVector2V(
  float x, float y);

void printVector2(
  Vector2 *vec);

void printRectangle(
  Rectangle *rect);

void num_to_3chars(
  char *s, int value);

#endif // !H_UTILS
